--- a/srt/entrypoints/engine.py
+++ b/srt/entrypoints/engine.py
@@ -131,6 +131,20 @@
         self.tokenizer_manager = tokenizer_manager
         self.scheduler_info = scheduler_info
 
+        # DeltaServe Phase 6: optionally spawn the backward subprocess. MPS env
+        # (CUDA_MPS_ACTIVE_THREAD_PERCENTAGE) is applied to the child only.
+        self.backward_process = None
+        if getattr(server_args, "enable_finetuning", False):
+            from sglang.srt.deltaserve.backward_process import spawn_backward_process
+            from sglang.srt.utils import get_open_port
+            bwd_port = get_open_port()
+            self.backward_channel_addr = f"tcp://127.0.0.1:{bwd_port}"
+            self.backward_process = spawn_backward_process(
+                channel_addr=self.backward_channel_addr,
+                model_name=server_args.model_path,
+                mps_pct=getattr(server_args, "backward_mps_percentage", 10),
+            )
+
         context = zmq.Context(2)
         self.send_to_rpc = get_zmq_socket(
             context, zmq.DEALER, port_args.rpc_ipc_name, True
@@ -547,6 +561,24 @@
     server_args.check_server_args()
     _set_envs_and_config(server_args)
 
+    # DeltaServe Phase 6: spawn backward subprocess BEFORE scheduler so the
+    # MPS env is active when the scheduler initializes CUDA context.
+    _deltaserve_backward_process = None
+    if getattr(server_args, "enable_finetuning", False):
+        from sglang.srt.deltaserve.backward_process import spawn_backward_process
+        from sglang.srt.utils import get_open_port
+        _bwd_port = get_open_port()
+        _bwd_channel_addr = f"tcp://127.0.0.1:{_bwd_port}"
+        _deltaserve_backward_process = spawn_backward_process(
+            channel_addr=_bwd_channel_addr,
+            model_name=server_args.model_path,
+            mps_pct=getattr(server_args, "backward_mps_percentage", 10),
+        )
+        logger.info(
+            f"[DeltaServe] backward subprocess spawned: pid={_deltaserve_backward_process.pid}, "
+            f"channel={_bwd_channel_addr}, mps_pct={server_args.backward_mps_percentage}"
+        )
+
     # Allocate ports for inter-process communications
     if port_args is None:
         port_args = PortArgs.init_new(server_args)
--- a/srt/entrypoints/http_server.py
+++ b/srt/entrypoints/http_server.py
@@ -157,6 +157,43 @@
     return Response(status_code=200)
 
 
+# DeltaServe Section 11: /start_finetuning endpoint. When
+# `finetune.start_on_launch=False` (the default under co-serving), FT
+# admission is gated off at launch — profiling + warmup get a clean GPU.
+# Eval harnesses POST this AFTER warmup to open admission.
+@app.post("/start_finetuning")
+async def start_finetuning():
+    from sglang.srt.deltaserve import gates as _ds_gates
+    _ds_gates.set_finetuning_started(True)
+    return ORJSONResponse({"finetuning_started": True})
+
+
+@app.post("/stop_finetuning")
+async def stop_finetuning():
+    from sglang.srt.deltaserve import gates as _ds_gates
+    _ds_gates.set_finetuning_started(False)
+    return ORJSONResponse({"finetuning_started": False})
+
+
+@app.get("/finetuning_status")
+async def finetuning_status():
+    from sglang.srt.deltaserve import gates as _ds_gates
+    return ORJSONResponse({"finetuning_started": _ds_gates.is_finetuning_started()})
+
+
+@app.get("/finetune_stats")
+async def finetune_stats():
+    # Read stats from the shared file (the scheduler subprocess writes it
+    # after each faux backward fires).
+    import os, tempfile, json
+    path = os.path.join(tempfile.gettempdir(), "sglang_ds_gates", "faux_stats.json")
+    try:
+        with open(path) as f:
+            return ORJSONResponse(json.load(f))
+    except (FileNotFoundError, json.JSONDecodeError):
+        return ORJSONResponse({"backward_calls": 0, "backward_total_ms": 0.0})
+
+
 @app.get("/health_generate")
 async def health_generate(request: Request) -> Response:
     """Check the health of the inference server by generating one token."""
--- a/srt/managers/io_struct.py
+++ b/srt/managers/io_struct.py
@@ -89,6 +89,8 @@
     modalities: Optional[List[str]] = None
     # LoRA related
     lora_path: Optional[Union[List[Optional[str]], Optional[str]]] = None
+    # DeltaServe: tag this request as a finetuning sample.
+    is_finetuning: bool = False
 
     # Session info for continual prompting
     session_params: Optional[Union[List[Dict], Dict]] = None
@@ -445,6 +447,8 @@
 
     # LoRA related
     lora_path: Optional[str] = None  # None means just use the base model
+    # DeltaServe: finetuning tag propagated from GenerateReqInput.
+    is_finetuning: bool = False
     # The input embeds
     input_embeds: Optional[Union[List[List[List[float]]], List[List[float]]]] = None
 
--- a/srt/managers/scheduler.py
+++ b/srt/managers/scheduler.py
@@ -191,6 +191,22 @@
     ):
         # Parse args
         self.server_args = server_args
+        # DeltaServe Phase 1/7: key off ServerArgs.enable_finetuning. When True,
+        # build a FinetuneConfig and attach the FinetuneSchedulerMixin via runtime
+        # class swap. Otherwise no behavior change (inference fast path identical).
+        self.finetune_config = None
+        self.finetune_coordinator = None
+        if getattr(server_args, "enable_finetuning", False):
+            from sglang.srt.configs.finetune import FinetuneConfig
+            from sglang.srt.managers.finetune_coordinator import FinetuneCoordinator
+            from sglang.srt.managers.finetune_scheduler_mixin import FinetuneSchedulerMixin
+            self.finetune_config = FinetuneConfig(enable_finetuning=True)
+            self.__class__ = type(
+                "FinetuneScheduler", (FinetuneSchedulerMixin, self.__class__), {}
+            )
+            self.finetune_coordinator = FinetuneCoordinator(
+                self.finetune_config, backward_channel=None
+            )
         self.tp_rank = tp_rank
         self.pp_rank = pp_rank
         self.tp_size = server_args.tp_size
@@ -934,6 +950,23 @@
             )
             req.tokenizer = self.tokenizer
 
+            # DeltaServe Phase 3 + Section 11: propagate finetuning tag onto
+            # the Req — but only if FT admission is OPEN (the /start_finetuning
+            # gate). Lets warmup run with zero FT cost even when reqs carry
+            # is_finetuning=True from the harness.
+            if getattr(recv_req, "is_finetuning", False):
+                from sglang.srt.deltaserve.gates import is_finetuning_started
+                if is_finetuning_started():
+                    # Section 4: admit-rate throttle. SGLANG_DS_FT_ADMIT_RATE
+                    # (0.0-1.0) is the fraction of FT-tagged requests that
+                    # actually carry the FT tag through. 1.0 = all; 0.5 =
+                    # half; 0.0 = none. Lets the operator cap eager-prefill
+                    # load without changing the request stream.
+                    import os, random
+                    rate = float(os.environ.get("SGLANG_DS_FT_ADMIT_RATE", "1.0"))
+                    if rate >= 1.0 or random.random() < rate:
+                        req.is_finetuning = True
+
             if self.disaggregation_mode != DisaggregationMode.NULL:
                 # Invalid request for disaggregated mode
                 if recv_req.bootstrap_room is None:
@@ -1550,6 +1583,16 @@
             logger.info(f"Scheduler.run_batch sleep {self.forward_sleep_time}s")
             time.sleep(self.forward_sleep_time)
 
+        # DeltaServe Section 6: revoke GPU grant during prefill (inference
+        # pre-empts the backward at layer boundaries). No-op when FT
+        # disabled. Decode-only steps run with grant held (concurrent OK).
+        _ds_grant = None
+        if getattr(self, "finetune_coordinator", None) is not None:
+            from sglang.srt.deltaserve.gpu_grant import gpu_grant
+            _ds_grant = gpu_grant()
+            if batch.forward_mode.is_extend():
+                _ds_grant.revoke()
+
         # Run forward
         if self.is_generation:
             if self.spec_algorithm.is_none():
@@ -1611,6 +1654,12 @@
             ret = EmbeddingBatchResult(
                 embeddings=embeddings, bid=model_worker_batch.bid
             )
+
+        # DeltaServe Section 6: hand the GPU grant back to the backward
+        # after the prefill kernels are queued. Decode-only steps already
+        # had it. Fire-and-forget — no sync.
+        if _ds_grant is not None:
+            _ds_grant.grant()
         return ret
 
     def process_batch_result(
--- a/srt/managers/schedule_batch.py
+++ b/srt/managers/schedule_batch.py
@@ -443,6 +443,9 @@
     ):
         # Input and output info
         self.rid = rid
+        # DeltaServe Phase 3: finetuning tag set by Scheduler when the incoming
+        # TokenizedGenerateReqInput carries is_finetuning=True.
+        self.is_finetuning: bool = False
         self.origin_input_text = origin_input_text
         self.origin_input_ids_unpadded = (
             origin_input_ids_unpadded
@@ -1662,6 +1665,9 @@
             encoder_lens_cpu=self.encoder_lens_cpu,
             encoder_out_cache_loc=self.encoder_out_cache_loc,
             lora_paths=[req.lora_path for req in self.reqs],
+            is_finetuning_flags=[
+                bool(getattr(req, "is_finetuning", False)) for req in self.reqs
+            ],
             sampling_info=self.sampling_info,
             input_embeds=self.input_embeds,
             spec_algorithm=self.spec_algorithm,
@@ -1752,6 +1758,10 @@
     # Sampling info
     sampling_info: SamplingBatchInfo
 
+    # DeltaServe: per-req finetuning flags. ForwardBatch.init_new builds
+    # is_finetuning_mask from this list (None when no FT reqs are present).
+    is_finetuning_flags: Optional[List[bool]] = None
+
     # The input Embeds
     input_embeds: Optional[torch.tensor] = None
 
--- a/srt/managers/tokenizer_manager.py
+++ b/srt/managers/tokenizer_manager.py
@@ -559,6 +559,7 @@
                 bootstrap_port=obj.bootstrap_port,
                 bootstrap_room=obj.bootstrap_room,
                 lora_path=obj.lora_path,
+                is_finetuning=obj.is_finetuning,
                 input_embeds=input_embeds,
                 session_params=session_params,
                 custom_logit_processor=obj.custom_logit_processor,
--- a/srt/mem_cache/paged_allocator.py
+++ b/srt/mem_cache/paged_allocator.py
@@ -187,6 +187,18 @@
         self._kvcache = kvcache
         self.ret_values = torch.empty((), dtype=torch.int64, device=self.device)
 
+        # DeltaServe FinetuningStore hook: predicate used by alloc() to skip
+        # pages reserved for FT samples. Fast path is gated on _has_reserved so
+        # default behavior is byte-identical when unused.
+        self._reserved_pred = lambda i: False
+        self._has_reserved: bool = False
+
+    def set_reserved_predicate(self, pred) -> None:
+        """DeltaServe Phase 5: register a predicate (page_idx) -> bool that
+        marks pages reserved for FT samples; alloc() will skip them."""
+        self._reserved_pred = pred
+        self._has_reserved = True
+
     def available_size(self):
         return len(self.free_pages) * self.page_size
 
--- a/srt/model_executor/forward_batch_info.py
+++ b/srt/model_executor/forward_batch_info.py
@@ -252,6 +252,10 @@
     # For Qwen2-VL
     mrope_positions: torch.Tensor = None
 
+    # DeltaServe: per-row bool tensor marking finetuning-tagged samples.
+    # None on inference-only batches (preserves cuda-graph fast path).
+    is_finetuning_mask: Optional[torch.Tensor] = None
+
     @classmethod
     def init_new(
         cls,
@@ -296,6 +300,29 @@
             ).to(device, non_blocking=True),
         )
 
+        # DeltaServe: populate is_finetuning_mask iff any req is FT-tagged.
+        # The mask is per-TOKEN (not per-request) so it can index activations
+        # of shape [total_tokens, D] correctly. For decode each req contributes
+        # 1 token; for prefill each req contributes its extend_seq_len.
+        _ft_flags = getattr(batch, "is_finetuning_flags", None)
+        if _ft_flags is not None and any(_ft_flags):
+            # Choose per-req token counts: decode → all-1; prefill → extend_seq_lens.
+            if batch.forward_mode.is_decode_or_idle():
+                token_counts = [1] * len(_ft_flags)
+            else:
+                # prefill: extend_seq_lens has the per-req new-token counts.
+                _es = batch.extend_seq_lens
+                if _es is not None:
+                    token_counts = _es.tolist() if hasattr(_es, "tolist") else list(_es)
+                else:
+                    token_counts = [1] * len(_ft_flags)
+            per_token: list = []
+            for flag, n in zip(_ft_flags, token_counts):
+                per_token.extend([bool(flag)] * int(n))
+            ret.is_finetuning_mask = torch.tensor(
+                per_token, dtype=torch.bool
+            ).to(device, non_blocking=True)
+
         # For DP attention
         if batch.global_num_tokens is not None:
             ret.global_num_tokens_cpu = batch.global_num_tokens
--- a/srt/model_executor/model_runner.py
+++ b/srt/model_executor/model_runner.py
@@ -579,6 +579,47 @@
             f"mem usage={(before_avail_memory - after_avail_memory):.2f} GB."
         )
 
+        # DeltaServe Phase 4 + Section 1: attach FinetuneAccumulator forward
+        # hooks now that the model exists. Hooks fire when a FT-tagged batch
+        # runs (begin_step set externally) and capture per-sample activations
+        # for the backward.
+        if getattr(self.server_args, "enable_finetuning", False):
+            try:
+                from sglang.srt.deltaserve.accumulate import FinetuneAccumulator
+                self.finetune_accumulator = FinetuneAccumulator()
+                self.finetune_accumulator.register_hooks(self.model)
+                n_handles = len(self.finetune_accumulator._handles)
+                logger.info(
+                    f"[DeltaServe] FinetuneAccumulator attached: {n_handles} forward hooks"
+                )
+                # Section 2: pre-capture the backward CUDA graph NOW (during
+                # startup) so the first FT-bearing forward doesn't pay the
+                # one-time capture cost. Mirrors DeltaServe-vLLM's
+                # "prepare()" at child startup.
+                try:
+                    from sglang.srt.deltaserve.faux_backward import precapture_graph
+                    precapture_graph(self.model)
+                except Exception as e:
+                    logger.warning(f"[DeltaServe] bwd graph pre-capture skipped: {e}")
+                # Task A / S12a: build the real backward when SGLANG_DS_REAL_BACKWARD=1.
+                # SGLANG_DS_BACKWARD_SUBPROCESS=1 runs it in an MPS-capped child
+                # (isolated SMs, scheduler thread stays free); else in-process Path C.
+                self._ds_bwd_client = None
+                try:
+                    from sglang.srt.deltaserve.real_backward import is_enabled, build_real_backward
+                    if is_enabled():
+                        from sglang.srt.deltaserve.backward_client import maybe_start_backward_subprocess
+                        self._ds_bwd_client = maybe_start_backward_subprocess(self.model)
+                        if self._ds_bwd_client is None:
+                            build_real_backward(self.model)  # in-process fallback
+                except Exception as e:
+                    logger.warning(f"[DeltaServe] real backward build failed: {e}")
+            except Exception as e:
+                logger.warning(f"[DeltaServe] accumulator attach failed: {e}")
+                self.finetune_accumulator = None
+        else:
+            self.finetune_accumulator = None
+
         # Handle the case where some ranks do not finish loading.
         try:
             dist.monitored_barrier(
@@ -1165,6 +1206,26 @@
     ) -> Tuple[Union[LogitsProcessorOutput, PPProxyTensors], bool]:
         self.forward_pass_id += 1
 
+        # DeltaServe Phase 4: arm the accumulator BEFORE the forward when
+        # the batch carries FT samples. Hooks fire inside model.forward and
+        # capture per-sample activations.
+        _ft_mask = getattr(forward_batch, "is_finetuning_mask", None)
+        _has_ft = _ft_mask is not None and bool(_ft_mask.any().item())
+        # DeltaServe: capture activations + run the backward ONLY on the FT
+        # *prefill* (the EXTEND forward where the sample's tokens form a real
+        # sequence). FT requests also carry FT-tagged tokens through their
+        # decode steps, but firing the backward there trains on unrelated
+        # single tokens AND adds ~44ms/step of GPU contention — 94% of fires
+        # were spurious decode fires, driving a 30x co-serving latency blowup.
+        # SFT is forward-only: exactly one backward per FT sequence (its prefill).
+        _ft_prefill = _has_ft and forward_batch.forward_mode.is_extend()
+        if _ft_prefill and getattr(self, "finetune_accumulator", None) is not None:
+            self.finetune_accumulator.begin_step(
+                req_ids=[],  # not needed for the math
+                finetune_mask=_ft_mask,
+                input_ids=forward_batch.input_ids,
+            )
+
         with get_global_expert_distribution_recorder().with_forward_pass(
             self.forward_pass_id,
             forward_batch,
@@ -1176,6 +1237,22 @@
         if self.eplb_manager is not None:
             self.eplb_manager.on_forward_pass_end(self.forward_pass_id)
 
+        # DeltaServe Path C: synchronously run faux-backward in-process so
+        # we measure realistic co-serving GPU contention. Real backward
+        # math wiring follows in next loop iteration (Task A).
+        if _ft_prefill and getattr(self, "finetune_accumulator", None) is not None:
+            try:
+                snapshot = self.finetune_accumulator.pop_step()
+                if getattr(self, "_ds_bwd_client", None) is not None:
+                    # S12a: ship to the MPS-capped backward subprocess (non-blocking,
+                    # drops if the child is still busy). Scheduler thread never waits.
+                    self._ds_bwd_client.submit(snapshot)
+                else:
+                    from sglang.srt.deltaserve.faux_backward import run_faux_backward
+                    run_faux_backward(snapshot, self.model)
+            except Exception as e:
+                logger.warning(f"[DeltaServe] backward dispatch failed: {e}")
+
         return output
 
     def _forward_raw(
@@ -1184,10 +1261,16 @@
         skip_attn_backend_init: bool,
         pp_proxy_tensors: Optional[PPProxyTensors],
     ) -> Tuple[Union[LogitsProcessorOutput, PPProxyTensors], bool]:
+        # DeltaServe: FT-tagged batches force eager forward so per-sample
+        # activation hooks can fire. v0.4.6 has no BreakableCudaGraphRunner;
+        # the eager path is the only way to make hooks observable.
+        _ft_mask = getattr(forward_batch, "is_finetuning_mask", None)
+        _has_ft = _ft_mask is not None and bool(_ft_mask.any().item())
         can_run_cuda_graph = bool(
             forward_batch.forward_mode.is_cuda_graph()
             and self.cuda_graph_runner
             and self.cuda_graph_runner.can_run(forward_batch)
+            and not _has_ft
         )
         if can_run_cuda_graph:
             ret = self.cuda_graph_runner.replay(
--- a/srt/server_args.py
+++ b/srt/server_args.py
@@ -220,6 +220,11 @@
     disaggregation_ib_device: Optional[str] = None
     pdlb_url: Optional[str] = None
 
+    # DeltaServe co-serving (LoRA finetuning interleaved with inference)
+    enable_finetuning: bool = False
+    backward_mps_percentage: int = 10
+    finetune_config: Optional[str] = None
+
     def __post_init__(self):
         # Expert parallelism
         if self.enable_ep_moe:
@@ -1416,6 +1421,25 @@
             help="Set multimodal attention backend.",
         )
 
+        # DeltaServe co-serving (inference + LoRA finetuning on the same GPU)
+        parser.add_argument(
+            "--enable-finetuning",
+            action="store_true",
+            help="Enable DeltaServe co-serving (inference + LoRA finetuning).",
+        )
+        parser.add_argument(
+            "--backward-mps-percentage",
+            type=int,
+            default=ServerArgs.backward_mps_percentage,
+            help="CUDA MPS active-thread percentage for the backward (SFT) process.",
+        )
+        parser.add_argument(
+            "--finetune-config",
+            type=str,
+            default=ServerArgs.finetune_config,
+            help="Path to a YAML file with FinetuneConfig overrides.",
+        )
+
     @classmethod
     def from_cli_args(cls, args: argparse.Namespace):
         args.tp_size = args.tensor_parallel_size
