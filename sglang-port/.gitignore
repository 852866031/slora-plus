.pytest_cache/
__pycache__/
*.pyc
# Optional upstream sglang checkout at the top of sglang-port/ — don't commit it.
# (The port is installed via v046-port/install.sh against a stock sglang, not a
# vendored source checkout.)
/sglang/
sglang-port/v046-port/sglang-fork/sglang.egg-info/
